### Main Idea
It's the process of applying the optical flow to transform (**Wrapping**) an image so it align with another frame
	Example:
		take the frame $t-1$, apply the [[Optical flow]], and produce a **wrapped** version such that it matches the frame  $t$
	$$ Warp(G_{t−1}​\ ,Flow_{t−1→t}​) $$
	